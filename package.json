{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npx pnpm -r --parallel --filter @workspace/api-server --filter @workspace/bistro-claytopia run dev",
    "dev:client": "npx pnpm --filter @workspace/bistro-claytopia run dev",
    "dev:server": "npx pnpm --filter @workspace/api-server run dev",
    "build": "npx pnpm run typecheck && npx pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npx pnpm run typecheck:libs && npx pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
